"use client";

import { PosterFallback } from "@/components/sketch/PosterFallback";

export function ReducedMotionPoster() {
  return (
    <PosterFallback
      src="/posters/coordination-engine.svg"
      alt="The Coordination Engine poster — concentric rings of agents linked by signal edges."
    />
  );
}
